---
import MainLayout from "../layouts/MainLayout.astro";
import ContactForm from "../components/ContactForm.jsx";
import OfficeLocator from "../components/OfficeLocator.jsx";
---

<MainLayout title="Contacto - RIBEIRO SRL">
    <!-- Cinematic Hero Section -->
    <section class="relative pt-44 pb-32 bg-black overflow-hidden">
        <div class="absolute inset-0 z-0">
            <img
                src="/assets/images/contacto/base.jpg"
                alt="Ribeiro Contacto"
                class="w-full h-full object-cover opacity-50"
            />
            <div
                class="absolute inset-0 bg-gradient-to-t from-black via-black/40 to-transparent"
            >
            </div>
        </div>

        <div class="container mx-auto px-6 relative z-10">
            <div class="max-w-3xl">
                <div
                    class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 border border-primary/20 text-primary mb-6 backdrop-blur-md animate-blur-in"
                >
                    <span class="text-sm font-bold uppercase tracking-wider"
                        >Contacto</span
                    >
                </div>
                <h1
                    class="text-5xl md:text-7xl font-extrabold text-white mb-6 leading-tight animate-blur-in delay-200"
                >
                    Estamos <br />
                    <span class="text-primary">conectados</span>
                </h1>
                <p
                    class="text-xl text-gray-300 max-w-2xl leading-relaxed animate-blur-in delay-400"
                >
                    ¿Tenés alguna consulta o proyecto en mente? Ponete en
                    contacto con nuestro equipo de profesionales. Estamos para
                    asesorarte.
                </p>
            </div>
        </div>
    </section>

    <!-- Office Locator Section -->
    <section class="py-24 bg-[#F9F9F9] relative overflow-hidden">
        <div
            class="absolute top-0 right-0 w-1/3 h-full bg-primary/5 -skew-x-12 translate-x-1/2"
        >
        </div>
        <div class="container mx-auto px-6 relative z-10">
            <div class="max-w-6xl mx-auto scroll-animate opacity-0">
                <div class="mb-12">
                    <h2 class="text-4xl md:text-5xl font-bold text-black mb-4">
                        Nuestras Oficinas
                    </h2>
                    <p class="text-gray-600 text-lg">
                        Seleccioná una provincia para encontrar nuestra base más
                        cercana.
                    </p>
                </div>
                <OfficeLocator client:load />
            </div>
        </div>
    </section>

    <!-- Form Section -->
    <section class="py-24 bg-white relative">
        <div class="container mx-auto px-6 relative z-10">
            <div class="max-w-3xl mx-auto scroll-animate opacity-0">
                <div class="mb-12 text-center">
                    <h2 class="text-4xl font-bold text-black mb-4">
                        Envianos un mensaje
                    </h2>
                    <p class="text-gray-600">
                        Completá el formulario y un asesor se contactará con vos
                        a la brevedad.
                    </p>
                </div>

                <div
                    class="bg-[#F9F9F9] p-10 md:p-14 rounded-[3.5rem] shadow-2xl shadow-black/5 border border-gray-100"
                >
                    <ContactForm client:load />
                </div>
            </div>
        </div>
    </section>
</MainLayout>

<style>
    .custom-scrollbar::-webkit-scrollbar {
        width: 6px;
    }
    .custom-scrollbar::-webkit-scrollbar-track {
        background: transparent;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: #e5e7eb;
        border-radius: 10px;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb:hover {
        background: #d1d5db;
    }
</style>
